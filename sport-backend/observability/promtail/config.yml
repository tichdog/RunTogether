server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: sport-backend
    static_configs:
      - targets:
          - localhost
        labels:
          job: sport-backend
          service: sport-backend
          env: development
          __path__: /var/log/sport-backend/*.log
    pipeline_stages:
      - json:
          expressions:
            level: level
            time: time
            msg: msg
            status: status
            service: service
            env: env
      - timestamp:
          source: time
          format: RFC3339Nano
      - labels:
          level:
          status:
          service:
          env:
